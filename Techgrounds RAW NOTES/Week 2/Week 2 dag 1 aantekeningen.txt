week 2/dag 1
shikha
this week we will finish linux and github

and then we will start with networking

this week :
knowledge check :LINUX

!!!NEXT MONDAY : SPRINT REVIEW!! 10 m inute ,  she gives the topic at wednesday.

--------------------------------------------------
personal 1on1 appointment: next week 

SPRINT presentation should not last more than 10 minutes
------------------
*****Add the scripts directory to the PATH variable.
*****
hoofdstuk 2/week 2

User
ubuntu Add the scripts directory to the PATH variable.
ChatGPT
To add the "scripts" directory to the PATH variable in Ubuntu, you'll need to modify your shell configuration file. Assuming you're using the default bash shell, you can do this by following these steps:

Open your terminal.

Identify which shell you are using by running:

bash
Copy code
echo $SHELL
This command will output the path to your current shell.

Edit the appropriate configuration file for your shell. For bash, it's typically ~/.bashrc or ~/.bash_profile. If you're using a different shell, the file may be different (e.g., ~/.zshrc for Zsh).

You can open the file using a text editor like nano or vi. For example:

bash
Copy code
nano ~/.bashrc
Add the following line at the end of the file:

bash
Copy code
export PATH="$PATH:/path/to/scripts"
Replace /path/to/scripts with the actual path to your scripts directory.

Save and close the file. In nano, you can do this by pressing Ctrl + O to write out, then Enter to confirm, and Ctrl + X to exit.

After saving the changes, you can either close and reopen your terminal or run the following command to apply the changes immediately:

bash
Copy code
source ~/.bashrc
Now, the "scripts" directory should be added to your PATH variable, and you should be able to execute scripts from that directory without specifying their full path.